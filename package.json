{
  "name": "krkn-website",
  "version": "1.0.0",
  "description": "Krkn chaos engineering documentation website with AI chatbot",
  "repository": "github:krkn-chaos/website",
  "homepage": "https://krkn-chaos.dev",
  "author": "Krkn Team",
  "license": "Apache-2.0",
  "scripts": {
    "_build": "npm run _hugo-dev --",
    "_check:links": "node scripts/check-links.js",
    "_hugo": "hugo --cleanDestinationDir",
    "_hugo-dev": "npm run _hugo -- -e dev -DFE",
    "_local": "npx cross-env HUGO_MODULE_WORKSPACE=docsy.work",
    "_serve": "npm run _hugo-dev -- --minify serve --renderToMemory",
    "build:preview": "npm run _build:index && npm run _hugo-dev -- --minify --baseURL \"${DEPLOY_PRIME_URL:-/}\"",
    "build:production": "npm run _build:index && npm run _hugo -- --minify",
    "_build:index": "node scripts/build-search-index.js",
    "build": "npm run _build -- ",
    "check:links": "npm run _check:links",
    "clean": "rm -Rf public/* resources",
    "local": "npm run _local -- npm run",
    "postbuild:preview": "npm run _check:links",
    "postbuild:production": "npm run _check:links",
    "serve": "npm run _serve",
    "test": "npm run check:links",
    "dev": "hugo server --buildDrafts --buildFuture"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cheerio": "^1.0.0-rc.12",
    "chokidar": "^3.5.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "fuse.js": "^7.0.0",
    "helmet": "^7.1.0",
    "marked": "^10.0.0",
    "natural": "^6.10.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "cross-env": "^7.0.3",
    "hugo-extended": "^0.146.0",
    "linkinator": "^6.3.0",
    "postcss-cli": "^11.0.0"
  },
  "private": true,
  "prettier": {
    "proseWrap": "always",
    "singleQuote": true
  }
}
